# Prometheus scrape job for bucket-scanner serve
# Merge into prometheus.yml scrape_configs:

- job_name: bucket-scanner
  static_configs:
    - targets: ["127.0.0.1:9090"]
  metrics_path: /metrics
  scrape_interval: 60s
  scrape_timeout: 10s
